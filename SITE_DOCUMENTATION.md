# التوثيق الرسمي لموقع عيادة د. ليان

**حالة الوثيقة:** إصدار تسليم أولي  
**المشروع:** `bassamdev711/doctor`  
**الفرع الموثق:** `main`  
**اللغة والاتجاه:** العربية، RTL  
**النطاق المقصود:** موقع عيادة أسنان احترافي مع نظام حجز ولوحة تحكم إدارية  
**تاريخ التوثيق:** 17 أغسطس 2026

> هذه الوثيقة تشرح البنية الوظيفية والبصرية والتقنية للموقع، وتربط كل واجهة مرئية بلقطات شاشة كاملة محفوظة داخل المستودع. لا تحتوي الوثيقة على كلمات مرور أو رموز جلسات أو مفاتيح اتصال.

## 1. الملخص التنفيذي

موقع عيادة د. ليان هو تجربة رقمية عربية فاخرة لعيادة متخصصة في طب الأسنان التجميلي والترميمي في **إب، اليمن**. صُممت الواجهة حول الهدوء والدقة والثقة، مع صورة Hero لضرس خزفي ثلاثي الأبعاد عائم، وتكوين RTL يترك مساحة النص الأساسية في الجهة اليمنى والعنصر البصري في الجهة اليسرى.

لا يقتصر المشروع على واجهة تعريفية؛ فهو يتضمن نظام حجز متصلًا بقاعدة بيانات PostgreSQL عبر Neon، ولوحة تحكم خاصة بالدكتور، وإدارة للخدمات والعروض والصور والحجوزات والمراجعات، مع رفع الصور إلى Vercel Blob. كما تتضمن طبقة الخادم تحققًا من المدخلات، وتحديدًا لمعدل الطلبات، ومنعًا للتكرار، وحماية لجلسات الإدارة.

## 2. هوية الموقع وتجربة الزائر

تستخدم الهوية البصرية درجات عاجية ورملية وفحمية ولمسات شامبانيا محدودة. لا تستخدم الواجهة رموزًا زخرفية أو أرقامًا تسلسلية ظاهرة للأقسام؛ العناوين نظيفة ومباشرة، بينما تُستخدم أيقونات SVG الرسمية عند الحاجة إلى إشارة بصرية.

| العنصر | الوصف المعتمد |
|---|---|
| الاسم | عيادة د. ليان |
| التخصص | طب الأسنان التجميلي والترميمي |
| المدينة والدولة | إب، اليمن |
| الهاتف | `+967 780 500 363` |
| اللغة | العربية |
| الاتجاه | RTL |
| صورة Hero | ضرس خزفي ثلاثي الأبعاد عائم |
| صورة المشاركة | `public/og-dental-clinic.jpg` |
| أيقونة الموقع | ضرس خزفي مخصص في `src/app/icon.png` |

تبدأ الصفحة العامة برسالة تعريفية ومسار واضح للحجز، ثم تنتقل إلى الخدمات، وفلسفة العيادة، ومعلومات الطبيبة، ومعرض الصور، وتجارب العملاء، ومعلومات التواصل. تم تصميم الحركة لتكون بطيئة وهادئة، مع عمق بصري محدود لا يشتت الانتباه عن الدعوة الأساسية إلى الحجز.

## 3. الصفحات والواجهات

| المسار | نوع الواجهة | الوظيفة |
|---|---|---|
| `/` | عامة | الصفحة الرئيسية الكاملة للزائر |
| `/admin` | خاصة | تسجيل دخول الدكتور ولوحة إدارة المحتوى والحجوزات |
| `/api/public/content` | API عامة | توفير الخدمات والعروض والصور والمراجعات المنشورة |
| `/api/bookings` | API عامة | استقبال طلبات الحجز والتحقق من عدم التكرار |
| `/api/reviews` | API عامة | استقبال تقييم العميل المرتبط بخدمة فعلية |
| `/api/admin/*` | API خاصة | إدارة الحجوزات والخدمات والعروض والصور والمراجعات والجلسة |

الصفحة العامة هي صفحة واحدة متكاملة وليست مجموعة صفحات تجريبية منفصلة. أما لوحة الدكتور فهي تطبيق إدارة داخل المسار `/admin`، وتستخدم تبويبات مستقلة داخل نفس الواجهة حتى تبقى إدارة العيادة سريعة وواضحة.

## 4. تجربة الحجز

يبدأ الحجز من زر واضح في الصفحة العامة، ثم يفتح نموذجًا يطلب البيانات الضرورية للموعد. تُرسل البيانات إلى قاعدة البيانات عبر المسار العام للحجز بعد فحص المخطط والتحقق الأمني. لا يُنشأ سجل مكرر عند النقر المزدوج أو إعادة إرسال الطلب نفسه، لأن الطلب يستخدم آلية idempotency في طبقة Neon.

يستطيع الدكتور رؤية الحجوزات داخل لوحة التحكم، ثم إدارة حالتها وفق سير العمل الداخلي للعيادة. لا تُعرض بيانات الحجوزات في الصفحة العامة، ولا تُحفظ حقول الحماية مثل honeypot أو request token ضمن سجل الحجز النهائي.

## 5. تجربة تقييم الخدمة

يرتبط كل تقييم بخدمة فعلية من قائمة الخدمات الموجودة في قاعدة البيانات. لا يمكن للعميل إرسال اسم خدمة حر أو غير موجود، كما يتحقق الخادم من أن الخدمة نشطة قبل قبول التقييم العام.

يتكون النموذج من اختيار الخدمة، وتقييم تفاعلي من خمس نجوم، وخانة ملاحظات يكتب فيها العميل تجربته. بعد الإرسال يرى العميل رسالة محايدة تؤكد وصول تجربته فقط، ولا تظهر له حالة داخلية مثل `pending` ولا أي عبارة تفيد بأن التقييم سيخضع للمراجعة.

تظل إدارة الحالة داخل لوحة الدكتور فقط. يستطيع الدكتور اعتماد التقييم أو إخفاءه أو رفضه أو حذفه، بينما تعرض الصفحة العامة التقييمات التي يسمح بها إعداد النشر فقط. هذا الفصل يحافظ على تجربة عميل بسيطة ويمنح العيادة التحكم التحريري المطلوب.

## 6. لوحة تحكم الدكتور

تتطلب لوحة التحكم جلسة إدارية موقعة ومحفوظة داخل HttpOnly Cookie. بعد تسجيل الدخول، تظهر الواجهة بتبويبات الإدارة التالية:

| التبويب | الوظائف |
|---|---|
| نظرة عامة | ملخص الحجوزات والنشاطات وإجراءات الإدارة السريعة |
| الحجوزات | عرض طلبات المواعيد وتحديث حالتها |
| الخدمات | إنشاء الخدمات وتعديلها وحذفها وإدارة صورها ووصفها |
| العروض والخصومات | إنشاء العروض وتعديلها وتفعيلها أو إخفاؤها |
| الصور والمعرض | رفع الصور من الجهاز وإدارة محتوى المعرض |
| آراء العملاء | إنشاء وتعديل واعتماد وإخفاء وحذف المراجعات |

يتم رفع الصور مباشرة من الجهاز إلى Vercel Blob بعد فحص نوع الملف وحجمه وتحديد معدل الرفع وقفل عمليات الرفع المتزامنة. لا تعتمد لوحة التحكم على إدخال روابط صور يدوية بوصفه المسار الأساسي.

## 7. قاعدة البيانات والتخزين

تستخدم طبقة البيانات PostgreSQL عبر Neon. تشمل الجداول الأساسية الحجوزات والخدمات والعروض والوسائط والمراجعات ورموز الطلبات. ترتبط المراجعات بالخدمات بواسطة `service_id`، مع إبقاء إمكانية التعامل مع بعض السجلات التاريخية القديمة التي قد لا تحتوي على علاقة مكتملة إلى أن تُراجع إداريًا.

يستخدم Vercel Blob لتخزين الصور المرفوعة من لوحة التحكم. يجب أن تكون متغيرات الاتصال مضافة في بيئات Vercel المناسبة، ولا ينبغي وضع قيمها داخل هذا الملف أو داخل المستودع.

## 8. التحصين الأمني

يطبق الموقع طبقة أمن مشتركة على المسارات العامة والإدارية. تشمل الحماية التحقق من مصدر الطلب، تحديد معدل الاستدعاءات، تقييد حجم الحمولة، منع التكرار، قفل الرفع المتزامن، وحماية تسجيل الدخول من محاولات brute force. كما تستخدم جلسة الإدارة توقيع HMAC وانتهاءً زمنيًا وخصائص Cookie آمنة.

تُضاف رؤوس حماية HTTP من إعدادات Next.js، بما في ذلك CSP وHSTS وX-Frame-Options وX-Content-Type-Options وPermissions-Policy وReferrer-Policy. تبقى الحماية الداخلية طبقة تطبيق، ولذلك يُنصح في بيئة الإنتاج بتفعيل جدار Vercel وBot Protection للحماية من الهجمات الموزعة واسعة النطاق.

## 9. دليل لقطات الشاشة

حُفظت لقطات الشاشة داخل مجلدين مستقلين تحت `docs/screenshots`، أحدهما للهاتف والآخر للابتوب. جميع اللقطات كاملة الطول للصفحة أو للحالة المرئية، وتحمل أسماء رقمية متطابقة بين المجلدين لتسهيل المقارنة.

### 9.1 لقطات الهاتف

المقاس الأساسي للعرض هو **390×844 CSS pixels**، مع التقاط الصفحة كاملة الطول عند الحاجة.

| الملف | الواجهة |
|---|---|
| [01-public-home.png](docs/screenshots/mobile/01-public-home.png) | الصفحة العامة كاملة |
| [02-booking-modal.png](docs/screenshots/mobile/02-booking-modal.png) | نموذج الحجز المفتوح |
| [03-review-form.png](docs/screenshots/mobile/03-review-form.png) | نموذج تقييم الخدمة والنجوم |
| [04-admin-login.png](docs/screenshots/mobile/04-admin-login.png) | شاشة دخول لوحة الدكتور |
| [05-admin-dashboard.png](docs/screenshots/mobile/05-admin-dashboard.png) | نظرة عامة في لوحة التحكم |
| [06-admin-bookings.png](docs/screenshots/mobile/06-admin-bookings.png) | تبويب الحجوزات |
| [07-admin-services.png](docs/screenshots/mobile/07-admin-services.png) | تبويب الخدمات |
| [08-admin-offers.png](docs/screenshots/mobile/08-admin-offers.png) | تبويب العروض والخصومات |
| [09-admin-media.png](docs/screenshots/mobile/09-admin-media.png) | تبويب الصور والمعرض |
| [10-admin-reviews.png](docs/screenshots/mobile/10-admin-reviews.png) | تبويب آراء العملاء |

### 9.2 لقطات اللابتوب

المقاس الأساسي للعرض هو **1440×1000 CSS pixels**، مع التقاط الصفحة كاملة الطول للصفحات الطويلة.

| الملف | الواجهة |
|---|---|
| [01-public-home.png](docs/screenshots/laptop/01-public-home.png) | الصفحة العامة كاملة |
| [02-booking-modal.png](docs/screenshots/laptop/02-booking-modal.png) | نموذج الحجز المفتوح |
| [03-review-form.png](docs/screenshots/laptop/03-review-form.png) | نموذج تقييم الخدمة والنجوم |
| [04-admin-login.png](docs/screenshots/laptop/04-admin-login.png) | شاشة دخول لوحة الدكتور |
| [05-admin-dashboard.png](docs/screenshots/laptop/05-admin-dashboard.png) | نظرة عامة في لوحة التحكم |
| [06-admin-bookings.png](docs/screenshots/laptop/06-admin-bookings.png) | تبويب الحجوزات |
| [07-admin-services.png](docs/screenshots/laptop/07-admin-services.png) | تبويب الخدمات |
| [08-admin-offers.png](docs/screenshots/laptop/08-admin-offers.png) | تبويب العروض والخصومات |
| [09-admin-media.png](docs/screenshots/laptop/09-admin-media.png) | تبويب الصور والمعرض |
| [10-admin-reviews.png](docs/screenshots/laptop/10-admin-reviews.png) | تبويب آراء العملاء |

> لقطات لوحة التحكم التُقطت بعد تسجيل دخول محلي مخصص للتوثيق. في بيئة التصوير المحلية لم تكن متغيرات Neon متاحة، ولذلك قد يظهر تنبيه «قاعدة البيانات غير مهيأة» داخل بعض لقطات الإدارة. هذا التنبيه يصف بيئة التصوير المحلية ولا يثبت حالة قاعدة بيانات Vercel الإنتاجية.

## 10. متطلبات التشغيل والإطلاق

| المتغير | الغرض |
|---|---|
| `ADMIN_EMAIL` | بريد دخول لوحة الدكتور |
| `ADMIN_PASSWORD` | كلمة مرور لوحة الدكتور |
| `AUTH_SECRET` أو `SESSION_SECRET` | توقيع جلسة الإدارة |
| `PGHOST` | عنوان Neon PostgreSQL |
| `PGUSER` | مستخدم قاعدة البيانات |
| `POSTGRES_PASSWORD` | كلمة مرور قاعدة البيانات |
| `POSTGRES_USER` | مستخدم PostgreSQL |
| `NEON_PROJECT_ID` | تعريف مشروع Neon عند الحاجة |
| `BLOB_READ_WRITE_TOKEN` | رفع الصور إلى Vercel Blob |

لا تُحفظ قيمة أي متغير سري في هذا الملف. يجب إدارة القيم من لوحة Vercel أو مدير أسرار مخصص، مع ضبط البيئات المطلوبة وفق مرحلة النشر.

## 11. التحقق قبل التسليم

تم تشغيل نسخة الإنتاج المحلية على المنفذ `3008` لالتقاط الصور، والتقطت المجموعة الكاملة في كلا المقاسين. تشمل المجموعة عشر لقطات للهاتف وعشر لقطات للابتوب، وتغطي الواجهة العامة، حالات النماذج، شاشة الدخول، وجميع تبويبات لوحة التحكم.

يجب قبل التسليم النهائي فتح النطاق العام على هاتف حقيقي ولابتوب حقيقي، وتجربة الحجز والتقييم ورفع صورة من لوحة الدكتور، ثم مشاركة الرابط عبر واتساب للتأكد من صورة Open Graph والأيقونة الجديدة بعد انتهاء التخزين المؤقت لدى المنصة.

## 12. مراجع المشروع الداخلية

هذا التوثيق مبني على ملفات المشروع الحالية، وأهم نقاط الرجوع التقنية هي:

[1]: src/app/layout.tsx "البيانات الوصفية وSEO والبيانات المنظمة"  
[2]: src/components/site-experience.tsx "الواجهة العامة وتجارب الحجز والتقييم"  
[3]: src/components/admin-dashboard.tsx "لوحة تحكم الدكتور"  
[4]: src/lib/db.ts "طبقة Neon ومخطط البيانات"  
[5]: src/lib/request-security.ts "طبقة تحديد المعدل ومنع التكرار والحماية"  
[6]: src/lib/admin-auth.ts "مصادقة جلسة الإدارة"  
[7]: next.config.ts "رؤوس حماية HTTP"  
[8]: docs/screenshots/mobile/ "لقطات الهاتف"  
[9]: docs/screenshots/laptop/ "لقطات اللابتوب"

تم إعداد هذه الوثيقة لتكون نقطة التسليم الرسمية داخل المستودع، مع فصل واضح بين معلومات التشغيل العامة والأسرار التي يجب أن تبقى في إعدادات Vercel فقط.
